import React from "react";
import { Container,Card, Image, Row, Col} from "react-bootstrap";
import myData from './menuItems.json';

const MenuDisplay = () =>{

    console.log(myData.length)
    
}

export default MenuDisplay;